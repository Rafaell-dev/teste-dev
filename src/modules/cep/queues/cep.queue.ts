/** Queue name shared between CepService (producer) and CepProcessor (consumer). */
export const CEP_QUEUE_NAME = 'cep-retry';

/** Job name for retry attempts. */
export const CEP_RETRY_JOB = 'retry-cep-lookup';
