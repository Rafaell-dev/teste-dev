/**
 * Default AppController was removed — the application has no root controller.
 *
 * Actual test coverage lives in:
 *   - src/modules/cep/controllers/cep.controller.spec.ts
 *   - src/modules/cep/services/cep.service.spec.ts
 *   - src/modules/cep/providers/viacep/viacep.provider.spec.ts
 *   - src/modules/cep/providers/brasil-api/brasil-api.provider.spec.ts
 *   - src/shared/cache/cache.service.spec.ts
 */

describe('AppModule', () => {
  it('is bootstrapped via main.ts', () => {
    // Placeholder — module-level tests are covered in feature spec files.
    expect(true).toBe(true);
  });
});
